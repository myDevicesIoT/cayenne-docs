// Generated by CoffeeScript 1.9.3
var assert, driver, drool, path, webdriver;

drool = require('drool');

assert = require('assert');

path = require('path');

driver = drool.start({
  chromeOptions: 'no-sandbox'
});

webdriver = drool.webdriver;

drool.flow({
  repeatCount: 5,
  setup: function() {
    return driver.get('file://' + path.join(__dirname, '/demo.html'));
  },
  action: function() {
    driver.findElement(webdriver.By.css('#add')).click();
    return driver.findElement(webdriver.By.css('#remove')).click();
  },
  assert: function(after, initial) {
    return assert.equal(initial.nodes, after.nodes, 'node count should match');
  }
}, driver);

driver.quit();
